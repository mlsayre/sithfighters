<%= form_for(@apprenticeship) do |f| %>
  <% if @apprenticeship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apprenticeship.errors.count, "error") %> prohibited this apprenticeship from being saved:</h2>

      <ul>
      <% @apprenticeship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.select :jedi_id, Jedi.all.collect { |p| [ p.name, p.id ] } %>
  </div>
  <div class="field">
    <%= f.select :padawan_id, Padawan.all.collect { |p| [ p.name, p.id ] } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
