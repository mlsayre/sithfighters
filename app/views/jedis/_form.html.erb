<%= form_for(@jedi) do |f| %>
  <% if @jedi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jedi.errors.count, "error") %> prohibited this jedi from being saved:</h2>

      <ul>
      <% @jedi.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
